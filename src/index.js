import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const appComponent = document.getElementById('root');
const root = ReactDOM.createRoot(appComponent);

root.render(<App />);